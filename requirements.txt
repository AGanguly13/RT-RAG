# RT-RAG dependencies (PyPI only — no local paths)
# Install: pip install -r requirements.txt
# Then: python -m spacy download en_core_web_sm

# API & HTTP
openai>=1.0.0
requests>=2.28.0

# NLP & retrieval (BM25 mode)
spacy>=3.7.0
scikit-learn>=1.3.0
rank_bm25>=0.2.2
numpy>=1.24.0
ijson>=3.2.0

# Data & async
datasets>=2.14.0
huggingface_hub>=0.20.0
aiofiles>=23.0.0
tqdm>=4.65.0

# Optional: dense retrieval + reranker (if METHOD=dense in config.py)
# Uncomment if you use dense retrieval:
# faiss-cpu>=1.7.0
# torch>=2.0.0
# transformers>=4.36.0

# Optional: local LLM serving (install on GPU node for vLLM)
# vllm>=0.4.0
